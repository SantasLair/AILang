{
  "name": "ailang",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p .",
  "start": "npm run build && node dist/src/main.js",
  "test": "npm run build && node dist/tests/compiler.test.js",
  "serve": "npm run build && node dist/src/server.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
